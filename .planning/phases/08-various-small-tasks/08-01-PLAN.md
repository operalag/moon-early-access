---
phase: 08-various-small-tasks
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/WelcomeScreen.tsx
  - src/app/api/news/route.ts
autonomous: true

must_haves:
  truths:
    - "Welcome page content appears higher on screen (reduced top gap)"
    - "News section displays exactly 4 items (not 10)"
    - "All other screens remain exactly as-is"
  artifacts:
    - path: "src/components/WelcomeScreen.tsx"
      provides: "Welcome screen with adjusted vertical positioning"
      contains: "pb-8"
    - path: "src/app/api/news/route.ts"
      provides: "News API returning 4 items"
      contains: ".slice(-4)"
  key_links:
    - from: "WelcomeScreen.tsx"
      to: "visual output"
      via: "Tailwind padding classes"
      pattern: "pb-\\d+"
    - from: "/api/news"
      to: "news page"
      via: "fetch response"
      pattern: "slice\\(-4\\)"
---

<objective>
Apply two targeted improvements: reduce welcome page top spacing and limit news feed to 4 items.

Purpose: Polish the app with user-requested spacing fix and cleaner news display.
Output: Modified WelcomeScreen.tsx and news route.ts with the specified adjustments.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/08-various-small-tasks/08-CONTEXT.md

# Source files to modify
@src/components/WelcomeScreen.tsx
@src/app/api/news/route.ts
</context>

<tasks>

<task type="auto">
  <name>Task 1: Reduce welcome page top spacing</name>
  <files>src/components/WelcomeScreen.tsx</files>
  <action>
Adjust the welcome screen to reduce the top gap (content should move up ~20-30%).

Current structure uses `justify-end pb-12` on the container and `p-8` on the content wrapper.

Change `pb-12` to `pb-8` on the outer container (line 12). This reduces bottom padding from 3rem to 2rem, which effectively moves content up by ~25%.

DO NOT change:
- Any spacing between elements inside the content
- Font sizes, colors, or button styles
- The overall layout structure
- Any other screens or components
  </action>
  <verify>
Visual check: Run `npm run dev`, open welcome screen, confirm content appears higher on screen than before (less gap at top of content area).
  </verify>
  <done>Welcome screen content positioned higher with reduced top gap. All internal element spacing unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Limit news feed to 4 items</name>
  <files>src/app/api/news/route.ts</files>
  <action>
Change the news API to return only 4 items instead of 10.

On line 33, change:
`const latestNews = matches.slice(-10).reverse();`

To:
`const latestNews = matches.slice(-4).reverse();`

This returns the latest 4 messages from the @cricketandcrypto channel instead of 10.

DO NOT change:
- The scraping logic
- The response format
- Error handling
  </action>
  <verify>
Run `curl http://localhost:3000/api/news | jq '.news | length'` and confirm it returns 4.
  </verify>
  <done>News API returns exactly 4 items. Response format unchanged.</done>
</task>

</tasks>

<verification>
1. `npm run build` succeeds with no TypeScript errors
2. Welcome screen visually shows content higher on screen
3. News API returns exactly 4 items: `curl localhost:3000/api/news | jq '.news | length'` outputs `4`
4. News page displays 4 news cards (not 10)
</verification>

<success_criteria>
- Welcome screen content moved up ~20-30% (reduced bottom padding)
- News section displays exactly 4 items
- No other screens or components modified
- Build passes without errors
</success_criteria>

<output>
After completion, create `.planning/phases/08-various-small-tasks/08-01-SUMMARY.md`
</output>
